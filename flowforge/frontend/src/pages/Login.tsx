import { FormEvent, useState } from 'react';
import { ArrowRight, LockKeyhole, Mail, Zap } from 'lucide-react';
import { login, register } from '../services/api';
export default function Login(){const [mode,setMode]=useState<'login'|'register'>('login');const [email,setEmail]=useState('demo@flowforge.local');const [password,setPassword]=useState('password');const [error,setError]=useState('');const submit=async(e:FormEvent)=>{e.preventDefault();setError('');try{mode==='login'?await login(email,password):await register(email,password);location.reload()}catch(err:any){setError(err?.response?.data?.detail||'Unable to authenticate')}};return <div className="auth"><div className="auth-glow"/><div className="auth-card"><div className="brand auth-brand"><div className="brandmark"><Zap size={18}/></div><div><strong>FlowForge</strong><span>Intelligent automation</span></div></div><div className="auth-copy"><div className="eyebrow">AUTOMATION CONTROL PLANE</div><h1>Build once.<br/><span>Automate everything.</span></h1><p>Orchestrate APIs, AI decisions and business workflows from one clean execution surface.</p></div><form onSubmit={submit}><label>Email <div className="input-wrap"><Mail size={16}/><input value={email} onChange={e=>setEmail(e.target.value)}/></div></label><label>Password <div className="input-wrap"><LockKeyhole size={16}/><input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></div></label>{error&&<div className="error">{error}</div>}<button className="primary full">{mode==='login'?'Enter workspace':'Create workspace'} <ArrowRight size={17}/></button></form><button className="switch" onClick={()=>setMode(mode==='login'?'register':'login')}>{mode==='login'?"New here? Create an account":"Already have an account? Sign in"}</button></div></div>}
